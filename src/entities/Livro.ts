export class Livro {
	nome: string;
	autor: string;
	preco: number;
	constructor(nome: string, autor: string, preco: number) {
		this.nome = nome
		this.autor = autor
		this.preco = preco
	}
}
